# Configuración no secreta consumida por docker-compose.production.yml.
# La conexión Atlas se guarda en mongo_produ/.env, nunca aquí.
CORS_ORIGINS=https://maletas.example.com
API_WORKERS=2
APP_PORT=8080

